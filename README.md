# hello-library

A small C++ library used to learn Git submodules and CI/CD integration.

## Structure

```text
hello-library/
├── include/
│   └── hello.h
├── src/
│   └── hello.cpp
└── README.md
```

The library exposes `get_hello_message()`, which returns a simple greeting.
